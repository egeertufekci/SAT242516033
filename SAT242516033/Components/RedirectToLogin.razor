@inject NavigationManager NavManager

@code {
    protected override void OnInitialized()
    {
        // Login sayfanın adresi neyse (örneğin: /login veya /Account/Login) buraya onu yaz
        NavManager.NavigateTo("/login", true);
    }
}